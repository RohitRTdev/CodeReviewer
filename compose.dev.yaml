services:
  frontend:
    build:
      target: dev

    volumes:
      - ./frontend:/app
      - /app/node_modules

    ports:
      - "8000:5173"

  backend:
    volumes:
      - ./backend:/app
      - /app/node_modules
    command: npm run dev

    